## Learned User Preferences

- Do not delete user comments in code when editing; preserve all existing comments
- Do not wrap code in tryCatch or other defensive error-handling unless explicitly requested; do not add output-suppression flags (`quiet`, `capture.output`) to hide warnings — keep all diagnostic output visible
- Assume required columns/variables always exist; do not write column-existence checks or fallback logic (e.g., `if ("col" %in% names(dt))`)
- Undersampling belongs in model_run scripts, not dataprep scripts; dataprep should output the full dataset
- Explain reasoning and present a plan before changing code; do not make code changes, run scripts/terminal checks, or perform validation runs unless explicitly asked; explicit regenerate/export requests count as authorization for the needed run
- Review plans carefully before implementing — user will ask for double-checks; never add items not in the plan
- Use raw (uncapped) variable names for report continuous vars (e.g., `cur_factor` not `c_cur_factor`, `dscr_ratio` not `c_dscr`) so reports show actual data distributions; for selected-deal/pool reports, honor age filters such as age 6-36, remove `deal_name` grouping for all-deal reports, use CLTV as a grouping/bucket when requested rather than assuming a PDF filter, and use equal loan weights in plots unless balance weighting is explicitly requested
- Only add model features that are statistically significant; do not add variables just because they seem relevant
- Store SQL queries in separate files from analysis scripts; put ad-hoc analysis under `Residential/non_qm/adhoc/`
- When presenting model output, pass only one `model_list` at a time to report functions; for insample reports use `ed_fit_full` (all data) with Begg normalization applied, dropping excluded states; turnover/refi model reports should reuse the same training weights via `apply_nqm_ctop_model_weights()` rather than resetting equal weights; default `use_parallel = TRUE` in optimized reporting where supported; if stopping parallel Begg/report jobs, kill orphaned PSOCK `Rscript.exe` workers and verify RAM/process cleanup before resuming; model tracking tables should be screenshot-style Excel output with separate CtoP/CtoM3 tabs, last 12 months, 3M/6M/12M error blocks, early date filtering, requested/default age windows (Fixed default 6-60), yellow `ALL AVG` rows outside conditional formatting, incentive/WAC/doc/jumbo sections, and Begg-softmax predictions (`p_Cto0_begg`, `p_CtoM3_begg`); for tieout debug snippets, put R and C++ values side by side using `R_*` and `Cpp_*` columns, followed by diff columns
- Keep time-series and overlay chart styling on one consistent path; avoid separate overlay-only palette/linetype helper layers unless necessary
- Use `S:\QR\R\R-4.3.2\bin\Rscript.exe` for R checks and export validation in this workspace

## Learned Workspace Facts

- NQM CtoP uses Begg competing-risk (Cto0, CtoM3, CtoC); Fixed is split into turnover and refi sub-models; ARM CtoP is a single `ctop_arm` prepayment model (no turnover/refi split); full Fixed CtoP reruns flow raw refresh -> LLPA -> shared prep -> fixed dataprep -> turnover -> update `TURN_MODEL_FILE` -> refi dataprep -> refi -> update `REFI_MODEL_FILE` -> JSON verify -> Begg reports/tracking; current v1.4 generated `.RData` artifacts save under `S:/QR/hzeng/temp/model` via `MODEL_DIR`; v1.4 targets centralize `CREDIT_NEXT_STATUS`, define `CtoM3` as current-to-credit-deterioration next statuses, and define `CtoC` strictly as `status == "C" & next_status == "C"` for Fixed and ARM; v1.4 tieouts mirror production prep: Fixed turnover scores the current fixed universe with `c_inc_0_spread_llpa` capped at `TURN_CUT_EST`, refi uses positive-incentive loans, Fixed refi residualization ranks actual Cto0 events with seeded `rnd_t_pred := runif(.N) * t_pred` by `r_dt/inc_bkt/pp_doc_f/Cto0` and uses `trunc(num_rows * turn_pct)` capped by `n_cto0` to classify turnover before fitting refi, ARM validates `ARM_CTOP_MODEL_PATH`, `ARM_CTM3_MODEL_PATH`, and `ed_arm_full.feather`, and all scoring copies set `s := 0`; full-bundle JSON/tracking can pass Fixed then fail if ARM paths still point at temp `MODEL_DIR` while the needed `.RData` files live under `Residential/non_qm/C/CtoP/models`; Fixed tieout prep pulls full selected-deal history through `NONQM_MULTI_DEAL_QUERY` into `P:/NQM/nqm_tieout/nonqm_tieout_prep_*`, uses 60-month burnout features, restricts to `orig_dt >= 2010-01-01`, skips C++ rows with no finite R model output after `!is.na(sum)` filtering, and should save optimized failed/top-diff PDF reports before failing on mismatches; flat-file tieout universes and raw fields come from unique `(pool_id, loan_id)` pairs in the monthly flat file, final C++ probabilities come from `simulation_outcomes.csv`, debug CSV `result` columns are raw logits joined by `pool_id + loan_id`, cached runs should read the fixed tieout prep parquet instead of re-pulling raw history, and `NQMFICO...` can be a pseudo-pool while `NQM_DEAL_FILTER_20260501.txt` has real DB pool IDs; flat-file CtoP selector mirrors C++: post-reset ARM (`fix_f == "ARM" & mon_sin_rst_v2 > 0`) uses floating, while fixed/non-post-reset rows use turnover only when `inc_0_spread_llpa <= 0` and turnover + refi otherwise; flat-file tieout output goes to `S:/QR/hzeng/temp/tieout`, and `raw_logits_softmax` joins C++ raw logits by `pool_id + loan_id`; Fixed/transition tieout PDFs should include summary pages plus requested categorical and continuous component detail plots; transition tieout uses `Residential/util/nonqm_tieout_report_utils.R` + `nonqm_transition_standard_tieout_v14.R` for Fixed CtoM3, ARM CtoM3, and ARM CtoP with PDF-first diagnostics and optional XLSX disabled by default; C++ `In:*` columns are for validation/reporting only and must not overwrite the R scoring prep; CtoC report needs `annulize = FALSE`
- `add_nqm_model_features()` `model_type` must match the model ("turnover", "refi", etc.) — there is no "Cto0" branch
- Non-QM LLPA uses a GAM (not a static FICO x LTV grid); doc groups FULL/BANKSTAT/DSCR/ALT/PL_CPA/OTHER; doc/fix normalization shared via helpers in `nonqm_settings.R` (`normalize_nqm_llpa_doc_map`, `get_nqm_llpa_fix_f`, `assign_nqm_doc_group`) to prevent train/serve drift; LLPA on lag1 rates, SATO and incentive use lag0/lowest-weekly; pure risk-grid predictions exclude time, in-sample keeps time with `orig_date_num` capped to training range; LLPA time smooth is `s(orig_date_num, k = 100, by = doc_group, bs = 'ts')` and exports through the standard TXT/JSON interaction-curve path (`orig_date_num.doc_group<LEVEL>_X/Y` mapping, no LLPA-only append/payload helpers); spline helpers use the full model data with no sampling, and JSON densifies fractional-year time curves like `llpa_time_doc_group` to complete monthly grids per selector min/max with missing `y` filled by interpolation; 2 SD residual trimming for SATO
- Incentive pipeline is spread-based: `inc_0_spread_llpa`, `sato_lag0_spread_llpa`, `burnout_lag0_spread_llpa`; `full_index_spread_fade15` replaced `full_index_inc_fade15`; turnover incentive capping targets `c_inc_0_spread_llpa`; ARM models use `rate_chg` (`c_noterate - o_noterate`) instead of `full_index_spread_fade15` (fade is always 1.0 for post-reset ARM; Fixed models still use `full_index_spread_fade15`); Fixed ARM/hybrid debug differences often come from index-rate date/month alignment because R joins `idx_rate` by `r_dt` + `lmsim_index_name` and fades `(effective_reset_rate - o_noterate)` using `mon_sin_rst_v2`; Non-QM index SQL emits `index_name` only (R derives `lmsim_index_name`) and maps `R1/R3/R6/RA/RZ/20`; blank flat-file `index_name` maps through `EMPTY -> LIBOR1Y` and must match R fallback logic to avoid `full_index` differences
- Turnover prepayment penalty: `inc_regime_f` includes `pp_active_Y`; `new_m2ppexp_f` bins months-to-PP-expiry for `pp_penalty == 1` (No PP plus ordered buckets through 4+)
- Model extract pipeline: TXT via `build_sim_model()` / `build_sim_model_from_list()`; JSON via `build_sim_model_json()` / `build_sim_model_json_from_list()` with product-specific schema+payload builder (production driver `nonqm_extract_model_json.R` outputs JSON only; `nonqm_model_json_compare.R` is a disposable parity harness); Fixed/flat-file C++ debug CSVs are not legacy LMSim dumps: read `loan_identifier`/`SimID`/`month_proj`, treat `result` columns as raw model logits, compare C++ `result` to R `sum` and `intercept_value` to `out_intercept`, and map `Out:*` using JSON schema component names (factor terms aggregate, `out_asofquarter` sums quarter `Out:` columns, CtoM3 `out_asofmonYYYYMM_f*` maps to `Out:asofmon_YYYYMM`, selector labels may preserve spaces); `variable_mapping_{product}_test.csv` maps R names to C++ names — any new factor-level coefficients (e.g. `*_fN`, `*_fY`) need explicit mapping rows or `verify_model_txt()` will fail; when a model uses a smooth without `by=` interaction (e.g. ARM CtoP's plain `s(c_inc_0_spread_llpa)`), the CSV needs plain `_X/Y` rows in addition to any interaction rows; CSV OLD column needs spaces stripped (`check.names=FALSE`); extract sources `C/config.R` for model paths; production C++ scoring uses logit (sigmoid); Boost.JSON 1.88 consumers are strict about numeric kinds, so JSON fields read as doubles must serialize decimal literals (`0.0`, not `0`); smooth component tieouts should clamp inputs to the exported JSON curve endpoints and should not use unrelated R/report caps or interpolation beyond the endpoint
- Fixed Balloon loans are ARM (`fix_f = "ARM"`) via NON_QM_FIX_MAP but often margin=0 — treat margin=0 as missing for ARM reset calculations
- DSCR doc_map: loans with `dscr_ratio` should be "DSCR" when doc_map is NA, "NO DATA", or "OTHER"; `dscr_valid_f` and `doc_type_2` can be near-collinear in GAM (bam contrast errors)
- ROUNDING_RULES in `resi_settings.R` must have a matching pattern for any new continuous variable used in reports
- Shared feature engineering (doc_map normalization, factor creation, penalty features, continuous clamps) lives in `fit_nonqm_data_cleaning_v1.0()`; tieout factor/input diagnostics should compare R factors such as `mod_f`, `fix_f`, `num_units_f`, and `dpd30times_f` to C++ `In:*` columns before debugging component values (`mod_f` is "Y" only when `mod_ft_pay_dt` is non-missing and `r_dt >= mod_ft_pay_dt`; Fixed CtoM3 `num_units_f` uses the `num_units >= 2` bucket, not `> 2`); FICO validity uses real-world range `300 <= ofico <= 850`, invalid FICO defaults to `c_ofico := 718`, and active v1.4 model-specific `c_ofico` overwrites should use `fifelse(fico_valid_f == "N", 718, ofico)` before clamping so invalid non-missing FICOs do not clamp to endpoints or become dropped NAs; model-type-specific branches stay in `add_nqm_model_features()`; when cleaning and model feature definitions conflict, the `add_nqm_model_features()` version wins; `c_age` is clamped 0–24 in shared cleaning but ARM post-reset loans are all age 60+, so ARM branches must override to `clamp_vec(age, 0, 120)`
- NQM burnout window 60 months (`burnout_inv_b2_60`); `inc_fade_period` 36; seasonality categorical (`month_f`); prefer `months_since_m30p` over `mc_age_ratio`; exclude COVID 2020-02–2020-06 from Fixed insample reports only (ARM models keep all COVID data and have no vintage floor filter); foreign national `lmt.foreign` 0–3, not binary; `apply_nqm_ctop_model_weights()` in `C/config.R` centralizes Fixed CtoP train/report weights and normalizes mean weight to 1; model training deal-call removal uses `pd_rate > 0.5 & n > 20`, while ad hoc deal-call diagnostics use cleaning logic (`deal_PD_mean > 0.95 & deal_count > 10`) plus last-reporting-month handling
- Data sources: loan-level `lps.lp_stat`, `lps.lp_dy`, `lps.lp_loss`; NQM rates `Libremax..MonthlyNQMRate`; HPI joins for inflation-adjusted o_bal
