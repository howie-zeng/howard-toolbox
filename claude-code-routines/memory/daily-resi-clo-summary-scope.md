---
name: daily-resi-clo-summary-scope
description: "Scope exclusions for the Daily RESI/CLO email summary task — what is NOT Howard's responsibility"
metadata:
  node_type: memory
  type: feedback
  originSessionId: 88006759-18e6-40fc-9f37-7d457d251d7b
---

For the Daily RESI/CLO Email Summary, Howard considers automated **risk runs** (the "LIBREMAX Risk Run" / "SWIB Risk Run ... scenario set" job notifications), **Resi SSS file generation** ("Daily Resi SSS File Generation"), and the **Compliance Engine** (Compliance@/Compliance_EOD@ started/stopped/failed) to be **out of scope** — ignore their failures/successes in the summary.

**Why:** These are run/owned by other people on the team, not Howard. Flagging their failures (e.g., a 2,156-bond SSS failure, CRT/CLO risk-run bond failures, a Compliance Engine crash) is noise for him.

**How to apply:** In the daily summary, exclude those three categories entirely. Focus instead on Howard's RESI/CLO model + report work: CRT model, Figure/DV01, LMSim vectors, CRTStats, factor_month/reporting_month (RESI); EUR/USD model training, V2/V2R/V3, spread models, walk-forward backtests, color/BWIC ingestion, model-fit, production comparison (CLO); plus surveillance/pricing/Intex-loader and Trimaran reports. The task file daily-email-summary.md was updated with an "Out of scope" note.

**Known benign weekend false-positive (learned 2026-07-20):** `[FAILURE] fundstudio database checker` (qrtest, in `Inbox/auto`) fires on **weekend dates** because its query `SELECT ... FROM Libremax.dbo.T_FS_PORTFOLIO WHERE PositionDate >= '<Sat/Sun date>'` returns no rows → `WARNING: database return no data for the date range`. This is the expected weekend no-position condition (FundStudio has no Sat/Sun portfolio snapshot), NOT a pipeline break, and it is FundStudio portfolio-data infra (fund-services/ops owned), not Howard's model. Do NOT flag it as a concerning failure. Only escalate if `T_FS_PORTFOLIO` is empty on a **business date** (a Monday+ empty result = real load failure). The sibling `[OK] intex database checker` (checks `INTEX_DEALMETRIC_TS`) is the CLO-side data-presence check and is the one to watch for CLO collateral loads.

**A lone `UNSTABLE: quant-*` in `/auto` is NOT evidence of an ongoing break — recovery is invisible to COM-only runs (learned 2026-08-03):** `quant-DailySimDataUpdateLP` finished UNSTABLE as `#88` (07-30) and `#92` (08-01), and the 08-01/08-02/08-03 summaries each escalated it as "no recovery signal / not confirmed recovered." Howard confirmed on 08-03 that the job **was back stable** — the recovering build reported **only to the Jenkins feed**, and a 120h `/auto` scan contains the two UNSTABLE emails and **no `Jenkins build is back to normal`** at all. Reason: qrprod emails UNSTABLE/FAILURE state-changes into the `/auto` report feed, but the subsequent SUCCESS/"back to normal" goes to `Jenkins Automation`, which is unreachable via COM (archive copies are ~1yr stale). **How to apply:** when the only evidence is an UNSTABLE 1+ days old with no newer email for that job, report it as **"UNSTABLE on <date>; recovery not verifiable from `/auto` — Jenkins feed unreachable this run"** and put it *below* genuinely new signals. Do NOT re-escalate the same build number as a fresh top-priority concern on consecutive days, and do NOT infer "still broken" from `/auto` silence — absence of a recovery email is the expected state, not a red flag. Ask/check Jenkins once, then let it drop unless a NEW build number goes UNSTABLE/FAILURE. See [[mailbox-folder-map]] for why the build feed is COM-invisible.

**Weekend report cadence — the deep ~02:05 overnight cluster is absent Sat/Sun mornings; this is NORMAL, not a failure (confirmed 2026-07-18 and 2026-07-25):** on a Saturday/Sunday run (covering the prior Friday/Saturday business date), the ~02:00–03:30 EDT report cluster — `CRT Monitor Report <BD>`, `All Funds Hedge & Summary Reports`, `Master Fund Portfolio Volatility` — is **not yet in the `/auto` feed as of ~03:51 (the batch top on weekends)**. Reduced weekend scheduling: these run later or not until the next business morning. Also skipped on weekends: `LMM OAS Pipeline`, `QL Forward Curves`, `Swaptions Update`, `Update Proxy KRD File`, `New Bonds Added`, and all `quant-*` RESI/CRT Jenkins pipelines. Report their absence as **"weekend cadence — expected, not a confirmed failure,"** NOT as a gap. Friday's own in-scope report jobs (CLO Loan Pricing/Surveillance, Loan Index, Intex loader chain, Listing/Galileo refresh, Trimaran, ColorDB, and — when they run — LMM OAS/QL Curves) still land during Friday and should show Success/OK; verify those.
