---
description: Project context for howard-toolbox
alwaysApply: true
---

# Howard Toolbox Rules

## Context Loading

Before non-trivial work, read `.cursor/skills/howard-toolbox/SKILL.md` (repo)
for full project knowledge. Domain corrections are in `.cursor/rules/memories.mdc`
(auto-injected, no read needed).

## Repo Locations

The project is used across multiple environments:
- **Company Machines**: The canonical repo lives at `S:\QR\hzeng\howard-toolbox`.
- **Local Desktop**: The repo is synced via OneDrive at `E:\OneDrive\Github\howard-toolbox`. The local desktop also hosts the OpenClaw Personal Cognitive OS at `C:\Users\Howard\.openclaw`.

Project skills and rules are tracked in the repo under `.cursor/`.
Global skills (`personal-profile`) live at `~/.cursor/skills/`.
